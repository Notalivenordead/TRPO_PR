<Window x:Name="PaymentWindow" x:Class="_222_Busin.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:_222_Busin"
        mc:Ignorable="d"
        Loaded="Window_Loaded"
        Closing="Window_Closing"
        Title="Учёт платежей" Height="450" Width="800">
    <Grid>
        <!--Объявление строк-->
        <Grid.RowDefinitions>
            <RowDefinition Height="45"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="45"></RowDefinition>
        </Grid.RowDefinitions>

        <!--Объявление столбцов-->
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="150"/>
        </Grid.ColumnDefinitions>

        <Button x:Name="BackButton" Content="Назад" Grid.Column="0" Margin="5,0,5,5" Click="BackButton_Click"/>
        <Button x:Name="ThemeButton" Grid.Column="2" 
                Content="Тёмная тема" Click="ThemeButton_Click" Margin="5,0,5,5"/>
        <Frame x:Name="MainFrame" Grid.Row="1" Grid.ColumnSpan="4" NavigationUIVisibility="Hidden"/>
        <TextBlock x:Name="DateTimeNow" Grid.Row="2" Grid.ColumnSpan="4" 
               HorizontalAlignment="Right" VerticalAlignment="Center" 
               Margin="0,0,10,0"/>
    </Grid>
</Window>
