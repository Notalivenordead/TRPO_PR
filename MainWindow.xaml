<Window x:Name="PaymentWindow" x:Class="_222_Busin.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:_222_Busin"
        mc:Ignorable="d"
        Loaded="Window_Loaded"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="45"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="45"></RowDefinition>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <Button x:Name="BackButton" Content="Назад" Grid.Column="0" Margin="10,12,621,13" Grid.ColumnSpan="2"/>
        <ToggleButton x:Name="ThemeToggle" Grid.Column="2" 
                  Content="Тёмная тема" Click="ThemeToggle_Click" VerticalAlignment="Top" Margin="0,13,0,0"/>
        <Button x:Name="ExitButton" Content="Закрыть" Grid.Column="3" Click="ExitButton_Click" 
            Margin="0,12,10,13"/>
        <Frame x:Name="MainFrame" Grid.Row="1" Grid.ColumnSpan="4" NavigationUIVisibility="Hidden"/>
        <TextBlock x:Name="DateTimeNow" Grid.Row="2" Grid.ColumnSpan="4" 
               HorizontalAlignment="Right" VerticalAlignment="Center" 
               Margin="0,0,10,0" Height="35" Width="194"/>
    </Grid>
</Window>
