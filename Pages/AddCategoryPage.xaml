<Page x:Class="_222_Busin.Pages.AddCategoryPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:_222_Busin.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Добавление/редактирование категории">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Название категории" FontSize="16" FontWeight="Bold" Margin="10"/>

        <TextBox x:Name="TBCategoryName" Grid.Row="1" 
                 Text="{Binding Name}" Margin="10" Height="30"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
            <Button Content="Сохранить" Click="ButtonSaveCategory_Click" Margin="5"/>
            <Button Content="Очистить" Click="ButtonClean_Click" Margin="5"/>
        </StackPanel>
    </Grid>
</Page>