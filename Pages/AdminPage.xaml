<Page x:Class="_222_Busin.Pages.AdminPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:_222_Busin.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Администратор">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Панель администратора" HorizontalAlignment="Center" FontSize="20" FontWeight="Bold" Margin="10"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
            <Button Content="Таблица пользователей" Click="BtnTab1_Click" Margin="5"/>
            <Button Content="Таблица категорий" Click="BtnTab2_Click" Margin="5"/>
            <Button Content="Таблица платежей" Click="BtnTab3_Click" Margin="5"/>
            <Button Content="Диаграммы" Click="BtnTab4_Click" Margin="5"/>
        </StackPanel>

        <Border Grid.Row="2" BorderBrush="LightGray" BorderThickness="1" Margin="10">
            <TextBlock Text="Выберите раздел для работы с данными" HorizontalAlignment="Center" VerticalAlignment="Center" 
                       FontSize="16" Foreground="Gray"/>
        </Border>
    </Grid>
</Page>