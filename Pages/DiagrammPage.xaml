<Page x:Class="_222_Busin.Pages.DiagrammPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:_222_Busin.Pages"
      xmlns:winForms="clr-namespace:System.Windows.Forms.Integration;assembly=WindowsFormsIntegration"
      xmlns:charting="clr-namespace:System.Windows.Forms.DataVisualization.Charting;assembly=System.Windows.Forms.DataVisualization"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Диаграммы платежей">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Панель управления -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
            <TextBlock Text="Пользователь:" VerticalAlignment="Center" Margin="5"/>
            <ComboBox x:Name="CmbUser" DisplayMemberPath="FIO" Width="200" Margin="5" SelectionChanged="UpdateChart"/>

            <TextBlock Text="Тип диаграммы:" VerticalAlignment="Center" Margin="5"/>
            <ComboBox x:Name="CmbDiagram" Width="150" Margin="5" SelectionChanged="UpdateChart"/>
        </StackPanel>

        <!-- Кнопки экспорта -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
            <Button Content="Экспорт в Excel" Click="ExportToExcel_Click" Margin="5"/>
            <Button Content="Экспорт в Word" Click="ExportToWord_Click" Margin="5"/>
        </StackPanel>

        <!-- Диаграмма -->
        <winForms:WindowsFormsHost Grid.Row="1" Margin="10">
            <charting:Chart x:Name="ChartPayments">
                <charting:Chart.Legends>
                    <charting:Legend Name="Legend1" Title="Легенда"/>
                </charting:Chart.Legends>
                <charting:Chart.ChartAreas>
                    <charting:ChartArea Name="ChartArea1"/>
                </charting:Chart.ChartAreas>
            </charting:Chart>
        </winForms:WindowsFormsHost>

    </Grid>
</Page>