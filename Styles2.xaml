<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:sys="clr-namespace:System;assembly=mscorlib">

    <!-- === ПАЛИТРА === -->
    <Color x:Key="Primary1Color">#2B2B2B</Color>
    <!-- фон окна -->
    <Color x:Key="Primary2Color">#3C3C3C</Color>
    <!-- фон страниц -->
    <Color x:Key="Accent1Color">#E0A9AF</Color>
    <!-- кнопки -->
    <Color x:Key="Accent2Color">#B5D5CA</Color>
    <!-- hover -->
    <Color x:Key="TextColor">#FFFFFF</Color>

    <!-- === КИСТИ === -->
    <SolidColorBrush x:Key="WindowBackgroundBrush" Color="{StaticResource Primary1Color}"/>
    <SolidColorBrush x:Key="PageBackgroundBrush" Color="{StaticResource Primary2Color}"/>
    <SolidColorBrush x:Key="ButtonBackgroundBrush" Color="{StaticResource Accent1Color}"/>
    <SolidColorBrush x:Key="ButtonHoverBrush" Color="{StaticResource Accent2Color}"/>
    <SolidColorBrush x:Key="ButtonBorderBrush" Color="#888888"/>
    <SolidColorBrush x:Key="TextBrush" Color="{StaticResource TextColor}"/>

    <FontFamily x:Key="AppFont">Segoe UI</FontFamily>
    <sys:Double x:Key="TitleFontSize">16</sys:Double>
    <sys:Double x:Key="BodyFontSize">13</sys:Double>

    <!-- === ГЛОБАЛЬНЫЕ СТИЛИ === -->

    <Style TargetType="Window">
        <Setter Property="Background" Value="{StaticResource WindowBackgroundBrush}"/>
        <Setter Property="FontFamily" Value="{StaticResource AppFont}"/>
        <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
    </Style>

    <Style TargetType="Page">
        <Setter Property="Background" Value="{StaticResource PageBackgroundBrush}"/>
        <Setter Property="FontFamily" Value="{StaticResource AppFont}"/>
        <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
    </Style>

    <!-- Заголовки -->
    <Style TargetType="TextBlock" x:Key="TitleText">
        <Setter Property="FontSize" Value="{StaticResource TitleFontSize}"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
        <Setter Property="Margin" Value="8"/>
    </Style>

    <!-- Текст -->
    <Style TargetType="TextBlock" x:Key="BodyText">
        <Setter Property="FontSize" Value="{StaticResource BodyFontSize}"/>
        <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
    </Style>

    <!-- Кнопки -->
    <Style TargetType="Button">
        <Setter Property="Height" Value="36"/>
        <Setter Property="Width" Value="140"/>
        <Setter Property="Margin" Value="8"/>
        <Setter Property="Padding" Value="6,3"/>
        <Setter Property="FontFamily" Value="{StaticResource AppFont}"/>
        <Setter Property="FontSize" Value="{StaticResource BodyFontSize}"/>
        <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
        <Setter Property="Background" Value="{StaticResource ButtonBackgroundBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource ButtonBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}" 
                  CornerRadius="8" 
                  BorderBrush="{TemplateBinding BorderBrush}" 
                  BorderThickness="{TemplateBinding BorderThickness}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="{StaticResource ButtonHoverBrush}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Opacity" Value="0.8"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- TextBox -->
    <Style TargetType="TextBox">
        <Setter Property="FontFamily" Value="{StaticResource AppFont}"/>
        <Setter Property="FontSize" Value="{StaticResource BodyFontSize}"/>
        <Setter Property="Margin" Value="5"/>
        <Setter Property="Padding" Value="4"/>
        <Setter Property="Background" Value="#444444"/>
        <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
        <Setter Property="BorderBrush" Value="#999999"/>
        <Setter Property="BorderThickness" Value="1"/>
    </Style>

    <!-- PasswordBox -->
    <Style TargetType="PasswordBox">
        <Setter Property="FontFamily" Value="{StaticResource AppFont}"/>
        <Setter Property="FontSize" Value="{StaticResource BodyFontSize}"/>
        <Setter Property="Margin" Value="5"/>
        <Setter Property="Padding" Value="4"/>
        <Setter Property="Background" Value="#444444"/>
        <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
        <Setter Property="BorderBrush" Value="#999999"/>
        <Setter Property="BorderThickness" Value="1"/>
    </Style>


    <!-- Подсказки -->
    <Style TargetType="ToolTip">
        <Setter Property="Background" Value="#444444"/>
        <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
        <Setter Property="BorderBrush" Value="#777777"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="6"/>
    </Style>

</ResourceDictionary>
